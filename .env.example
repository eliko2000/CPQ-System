# Smart CPQ System - Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ============================================================================
# REQUIRED: Supabase Configuration
# ============================================================================
# Get these from: https://supabase.com/dashboard > Project Settings > API
#
# IMPORTANT:
# - Use the ANON key, NOT the SERVICE ROLE key in client-side code
# - Service role keys bypass Row Level Security and should only be used server-side
# - Never commit actual keys to version control

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# ============================================================================
# REQUIRED: Anthropic Claude AI Configuration
# ============================================================================
# Get from: https://console.anthropic.com
# Used for: Intelligent document parsing and OCR
# Cost: ~$0.01-0.05 per document (images only)
#
# NOTE: If not provided, AI Vision features will be disabled
# Excel and CSV parsing will still work without this key

VITE_ANTHROPIC_API_KEY=sk-ant-api03-your-anthropic-api-key-here

# ============================================================================
# OPTIONAL: OCR Service Configuration
# ============================================================================
# Only needed if using external OCR service (currently unused)

# VITE_OCR_API_KEY=your-ocr-service-key

# ============================================================================
# OPTIONAL: Application Configuration
# ============================================================================
# These are optional and have sensible defaults

# Application name (for UI display)
VITE_APP_NAME=CPQ System

# Application version (semantic versioning)
VITE_APP_VERSION=1.0.0

# Environment (development, staging, production)
VITE_APP_ENV=development

# ============================================================================
# OPTIONAL: Feature Flags
# ============================================================================
# Enable or disable specific features

# Enable AI-powered document import (requires Anthropic API key)
VITE_ENABLE_AI_IMPORT=true

# Enable analytics tracking
VITE_ENABLE_ANALYTICS=false

# Enable debug mode (verbose logging, debug tools)
VITE_DEBUG_MODE=true

# ============================================================================
# SECURITY REMINDERS
# ============================================================================
# 1. Never commit .env.local to git
# 2. Rotate all keys if accidentally exposed
# 3. Use different keys for dev/staging/prod
# 4. Keep service role keys server-side only
# 5. Monitor API usage and set up billing alerts
