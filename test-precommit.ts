// Test file with intentional formatting issues
const badVariable = 'no spaces around equals';
const unused_variable = 'this will cause lint warning';

function badFormatting() {
  const x = 1 + 2;
  return x;
}

export const test = true;
